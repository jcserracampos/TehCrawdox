<%= form_for(@categoria) do |f| %>
  <% if @categoria.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categoria.errors.count, "error") %> prohibited this categoria from being saved:</h2>

      <ul>
      <% @categoria.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :nome %><br>
    <%= f.text_field :nome, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :descricao, 'Descrição' %><br>
    <%= f.text_area :descricao, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :ativo %><br>
    <%= f.check_box :ativo %>
  </div>
  <div class="form-group">
    <%= f.label :url, 'Link da categoria' %><br>
    <%= f.text_field :url, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :path, 'Caminho padrão para salvar' %><br>
    <%= f.text_field :path, class: 'form-control' %>
  </div>
  <div class="actions">
    <%= f.submit 'Cadastrar', class: 'btn btn-primary' %>
  </div>
<% end %>
