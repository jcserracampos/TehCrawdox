<%= render 'layouts/navbar' %>

<div class="container-fluid">

<h1>Editando <%= @host.titulo %></h1>

<%= render 'form' %>

<div class="actions">
<%= link_to 'Exibir o host', @host, class: 'btn' %>
<%= link_to 'Cancelar', hosts_path, class: 'btn btn-danger' %>
</div>
</div>